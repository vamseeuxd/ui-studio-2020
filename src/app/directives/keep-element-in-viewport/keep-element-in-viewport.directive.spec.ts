import { KeepElementInViewportDirective } from './keep-element-in-viewport.directive';

describe('KeepElementInViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepElementInViewportDirective();
    expect(directive).toBeTruthy();
  });
});
